import template from './abstract.html';
import controller from './abstract.controller';

const abstractComponent = {
    bindings: {},
    template,
    controller
  };

export default abstractComponent;
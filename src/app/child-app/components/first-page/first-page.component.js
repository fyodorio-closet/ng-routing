import template from './first-page.html';
import controller from './first-page.controller';
import './first-page.scss';

const firstPageComponent = {
    bindings: {},
    template,
    controller
  };

export default firstPageComponent;
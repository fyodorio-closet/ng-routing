import template from './parent-app.html';
import controller from './parent-app.controller';
import './parent-app.scss';

const parentAppComponent = {
    bindings: {},
    template,
    controller
  };

export default parentAppComponent;